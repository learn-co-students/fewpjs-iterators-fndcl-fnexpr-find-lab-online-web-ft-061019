const testVar = {}

function testFunc() {
  return "hi"
}

function superbowlWin(team){
  let yearResults = team.find(function(result) {
    return yearResults.result == "W"
   })
   if (yearResults){
     return result.year
   }
}
