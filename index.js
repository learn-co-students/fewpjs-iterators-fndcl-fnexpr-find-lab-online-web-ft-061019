const testVar = {}

function testFunc() {
  return "hi"
}

function superbowlWin(arrayOfObjects) {
 let banana = arrayOfObjects.find(function(arrayOfObjects) {return arrayOfObjects.result === "W"})
  if (banana) {
    return banana.year
  }
  
}